"""로봇이 말하는 고정 문구 모음 (LLM 이 만들지 않는 말).

한 곳에 모으는 이유는 두 가지다.

1. 자가 트리거 검사를 빠짐없이 돌리기 위해서다. 이 문구에 긴급어가 섞이면 상시
   긴급어 감시가 스피커로 나간 로봇 자기 목소리를 다시 긴급어로 인식한다.
   tests/test_spoken_text.py 가 이 모듈의 모든 문구를 자동으로 훑는다.
2. 시각장애인 사용자 대상 문구는 감수 대상이라, 흩어져 있으면 검토가 어렵다.

Mission Manager 의 안내 멘트(MSG_*)는 vica_ros2_ws 쪽에 있다. 그쪽은 주행 상태를
아는 노드가 말해야 하기 때문이며, 같은 성격의 검사를 별도로 두고 있다.
"""
from __future__ import annotations

# 긴급어 감지 시의 응답. 실제 정지는 E-stop 래치 체인이 수행한다.
EMERGENCY_REPLY = "안전을 위해 멈추겠습니다."

# 발화를 접수했다는 즉시 신호.
#
# LLM 응답은 네트워크 왕복이 있어 수 초가 걸린다. 그동안 아무 소리도 나지 않으면
# 눈으로 확인할 수 없는 사용자는 로봇이 들었는지 알 수 없어 같은 말을 반복하게
# 되고, 그러면 대기열이 밀린다. 짧게 한 번 답하는 편이 낫다.
# 문구 규칙: 호출 응답("네?")과 귀로 구분되고 긴급어와 겹치지 않아야 한다.
# 같은 말만 반복되면 지겨워서 풀에서 무작위로 고른다 (선택은 ros_node 몫).
# 문구 5종은 사용자 지정(2026-08-28). ⚠️ "잠시만요"는 일시정지 단축어와
# 동일 — 에코가 AEC 를 뚫고 user_text 로 돌아오면 자가 일시정지가 가능하다.
# 로봇이 말한 직후 혼자 멈추는 일이 관측되면 이 문구부터 뺄 것.
ACK_LISTENING_POOL = (
    "확인할게요.",
    "잠시만요.",
    "찾아볼게요.",
    "잠시 기다려주세요.",
    "알아볼게요.",
)

# STT 가 아무 말도 알아듣지 못했을 때. 침묵으로 두면 다시 말할 시점을 못 잡는다.
RETRY_PROMPT = "잘 듣지 못했습니다. 다시 말씀해 주세요."

# LLM 호출이 실패했을 때 (네트워크 등). 긴급어는 LLM 이전 단계에서 처리되므로
# 이 실패의 영향을 받지 않는다.
LLM_UNAVAILABLE = "죄송합니다. 지금은 요청을 처리할 수 없어요. 잠시 후 다시 말씀해 주세요."

# LLM 이 목록에 없는 목적지를 골랐을 때의 되묻기.
ASK_DESTINATION = "어디로 안내해드릴까요?"

# 확인 질문에 사용자가 "아니요"라고 답했을 때. 되묻기로 이어진다.
# 스마트핸들 모드를 거절했을 때도 같은 말이 맞으므로 새로 만들지 않고 이 상수를 쓴다.
CONFIRM_DECLINED = "알겠습니다. 어디로 안내해드릴까요?"

# --- 주행 제어 확인 질문 (cancel / pause / resume) ------------------------------
#
# LLM 은 제어 의도를 '제안'만 하고, 로봇이 아래 문구로 되물은 뒤 "네"를 코드가
# 확인해야 실행 경로(Mission Manager 의 MissionCommand 서비스)로 넘어간다.
#
# 확인은 위험 방향에만 건다: 임무를 버리는 cancel 과 움직임을 시작하는 resume 은
# 되묻고, 서는 방향인 pause 는 되묻지 않는다 (아래 PAUSE_ACK).
CANCEL_CONFIRM = "안내를 취소할까요?"
RESUME_CONFIRM = "다시 출발할까요?"

# --- 사람 접근 멘트 (사전녹음용 — scripts/make_cue_wavs.py 가 굽는다) ----------
#
# 접근 질문·온보딩은 고정 멘트라 매번 합성하지 않고 녹음을 재생한다 (2026-08-25
# 사용자 결정). 발화 주체는 Mission 이므로(수락 발화 규칙), 이 문구를 실제로
# 트는 배선은 TTS 문장 캐시 + Mission 문구 교체에서 완성된다.
#
# 주의: 온보딩에 웨이크워드 "비카야"가 들어 있다. 뮤트 모드에서는 재생 중
# 마이크가 닫혀 무해하지만, AEC 모드(VICA_TTS_MUTE=off)에서는 "온보딩 재생 중
# 자가 웨이크 0회"를 장착 검증 항목으로 반드시 확인한다.
APPROACH_QUESTION = (
    "안녕하세요? 저는 시각장애인 안내로봇 비카입니다! "
    "저와 함께 목적지까지 동행해보시는건 어떠세요? 안내를 받으시겠어요?"
)
APPROACH_ONBOARDING = (
    "안녕하세요? 반갑습니다! 저를 부르시려면 '비카야'라고 불러주시면 됩니다! "
    "자, 이제 어디로 가고싶으신가요?"
)

# 아래 셋의 발화 주체는 Mission(ros2)이다 — 정본이 mission_logic 으로 갈 예정이라
# 문구를 바꾸면 양쪽을 함께 고치고 재녹음한다(scripts/make_cue_wavs.py).
# 회전 예고는 시각장애인 안전 멘트다 — 예고 없는 움직임 금지 (2026-08-25 결정).
APPROACH_TURN_NOTICE = "네, 잠시만 기다려주세요. 로봇이 회전하니 주의하세요."
APPROACH_TURN_DONE = "회전이 완료되었습니다."
APPROACH_FAREWELL = "알겠습니다. 이만 물러납니다."

# pause 는 즉시 요청한다 — 현장 시험(2026-08-15)에서 로봇이 움직이면서 "잠시
# 멈출까요?"라고 되묻는 것은 부자연스러웠고, 서는 방향의 오분류는 잠시 서는 것
# 뿐이라 안전한 실패다. 실제 정지는 Mission Manager 가 짧은 거리 감속으로
# 수행해야 한다 (급정지는 E-stop 전용).
PAUSE_ACK = "네, 잠시 설게요."

# 첫 상호작용에서 로봇이 자기 어휘를 가르쳐 준다. 어휘를 모르는 사용자는
# 본능적으로 "멈춰"를 쓰는데, 그 말은 E-stop 이라 관리자 리셋까지 필요해진다
# (현장 시험 2026-08-15). '잠깐'은 소프트 키워드라 자가 트리거되지 않는다.
USAGE_GUIDE = "안내 중에 잠깐 쉬려면 '잠깐만', 다시 가려면 '다시 가자', 그만두려면 '취소'라고 말씀해 주세요."

# 제어 확인에 "아니요"라고 답했을 때. 하던 일을 계속한다는 뜻이므로, 목적지를
# 되묻는 CONFIRM_DECLINED 와 다른 문구가 필요하다.
COMMAND_DECLINED = "알겠습니다. 계속 진행할게요."

# --- 스마트핸들 모드 -----------------------------------------------------------
#
# 공용 로봇이라 사용자가 계속 바뀌고, 시각장애인만 쓰는 것도 아니다. 그래서 모드를
# 매번 물어야 한다.
#
# **묻는 시점은 목적지가 정해진 뒤다.** 호출("비카야")만으로는 안내를 원하는지 알 수
# 없다. 층을 묻거나 잘못 부른 사람에게까지 모드를 물으면 대화가 어긋난다. 안내
# 의사가 확인된 뒤에야 "그러면 손잡이를 쓰시겠습니까"가 성립한다.
#
# 문구는 vica_scenario.md 2-1.1 절의 예시를 그대로 쓰지 않는다. 그 예시의
# "핸들을 놓으시면 로봇이 잠시 후 정지하오니"는 긴급어 '정지'를 담고 있어
# 상시 긴급어 감시가 로봇 자기 목소리를 E-stop 으로 인식한다(자가 트리거).
# "멈춥니다"도 '멈춰'에 걸린다. 안전한 대체어는 "섭니다", "중단됩니다"다.
MODE_ASK = "손잡이를 잡고 스마트핸들 모드를 시작하시겠어요?"

# 모드 수락 → 진입 요청. 목적지는 이미 확정돼 있고 Mission Manager 가 들고 있다.
MODE_ENTER_REQUEST = "손잡이를 3초간 잡아 주세요."

# 터치 3초 확인 완료. 목적지를 다시 묻지 않는다 — 이미 알고 있다.
# 바로 뒤에 Mission Manager 의 MSG_START("{name}{josa} 안내를 시작합니다")가 잇는다.
#
# 모드 이름을 말해 진입을 알린다. 소리로만 상태를 아는 사용자에게는 이 한 마디가
# 손을 놓아도 되는지를 판단할 유일한 근거다.
MODE_READY = "스마트핸들 모드입니다."

# 모드 거절. 목적지는 이미 확정돼 있으므로 되묻지 않는다 — 바로 뒤에 Mission
# Manager 의 MSG_START 가 잇는다. 목적지 확인의 CONFIRM_DECLINED 와 달리
# ASK_DESTINATION 을 붙이지 않는 이유다.
MODE_DECLINED = "알겠습니다."

# 핸들 상향 통신이 끊긴 상태. 3초를 감지할 수단이 없어 활성화가 불가능하므로
# 아예 묻지 않는다(vica_scenario.md 2-1.1). 물어보면 사용자는 아무리 잡아도
# 왜 안 되는지 알 수 없다. 이 문구도 목적지 확정 뒤에 나오므로 되묻지 않는다.
HANDLE_UNAVAILABLE = "손잡이 연결에 문제가 있어 일반 안내로 진행합니다."

# 활성 모드에서 손을 놓았을 때의 반복 안내 (터치센서 도입 후 사용).
# vica_scenario.md 2-1.2 의 "핸들을 잡지 않으시면 로봇이 정지합니다"는 '정지'가
# 들어 있어 쓰지 않는다. 이미 서 있는 상태라 정지를 예고할 필요도 없다.
HANDLE_LOST = "손잡이를 잡아 주세요."

# 안내 한 건의 첫 호출에 답하는 말. 이후 호출은 짧은 음으로 답한다.
# 짧은 음은 시중 AI 스피커의 관습이라 대체로 통하지만, 처음 쓰는 사용자에게는
# 한 번 말로 알려준다 (2026-08-05 결정).
WAKE_GREETING = "네?"

# 회전 안내. 사용자의 안전과 직결되므로 축약하지 않고 매번 같은 문장을 말한다.
# 방향을 잘못 알면 넘어질 수 있다.
TURN_LEFT = "좌회전 할게요."
TURN_RIGHT = "우회전 할게요."


def all_phrases() -> dict[str, str]:
    """이 모듈이 가진 고정 문구 전체 (검사·감수용). 문구 묶음(tuple)도 편다."""
    phrases: dict[str, str] = {}
    for name, value in globals().items():
        if not name.isupper():
            continue
        if isinstance(value, str):
            phrases[name] = value
        elif isinstance(value, (tuple, list)):
            for i, item in enumerate(value):
                if isinstance(item, str):
                    phrases[f"{name}[{i}]"] = item
    return phrases
